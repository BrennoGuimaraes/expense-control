spring:
  application:
    name: expensecontrol
  datasource:
    url: jdbc:postgresql://localhost:5432/finance_db
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect